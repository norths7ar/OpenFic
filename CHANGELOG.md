# 更新记录

本文件记录用户可感知的功能变化和修复。未发布的变化先归入 `Unreleased`，发布时再整理版本号与日期。

## Unreleased

### 修复

- 修正资料变更提案将未修改字段的 `null`、新建时无关字段的默认占位误判为参数错误；保留空补丁与有实际内容的不适用字段校验。
- 提供商探测列表不再显示远端未返回且已关闭的模型；保留本地配置，仍启用的缺失模型继续显示提示。

## v1.1.0 — 2026-09-09

### 功能

- 五类文档支持持久化历史预览与恢复：连续手工保存按 5 分钟合并，每篇保留最近 100 个恢复点，AI、导入、应用待审及恢复操作前单独留点；历史独立于讨论任务。
- 增加项目回收站，可预览、恢复和确认永久删除条目；回收站不自动清空，原文件夹缺失时恢复到对应类型根目录。
- 编辑页讨论区的资料范围移入输入框连体顶栏；待审入口仅在有待审变更时显示，直接进入完整待审页；最近任务增加卡片边框、行分隔和悬停反馈。
- 写作、背景设定、角色、提纲和笔记使用公共工作区及多标签；按项目和页面恢复已打开条目、当前标签和滚动位置。关闭最后一个标签后保持空态，不再自动生成空标签。
- 编辑工具栏支持可视化／源码切换，默认可视化且不记忆模式；资料页支持 Markdown 源码编辑和跨模式撤销，正文保留纯段落语义。
- 五个编辑页复用分栏及编辑框架模板，统一讨论区控制按钮；桌面默认展开，收起状态在本次使用期间跨页共享，刷新后恢复展开。
- Markdown 源包 v2 使用目录映射和专用条目标记，支持每条目一个文件或同目录合并。正文标题不再承担条目边界，文件夹归属由相对目录表达；旧 v1 包继续支持导入，新导出统一采用 v2。

### 修复

- 工具“自动审批”更名为“免确认执行”，明确全局作用范围；启用后仍遵守工具的禁止规则。
- 修正写作编辑区未撑满工作区、状态栏下方出现大块空白的问题。
- 移除提纲和笔记重复的顶部标题栏，以及写作工具栏中的重复“世界书”导航入口。
- 修正待审页详情空态未占满剩余工作区造成的居中偏位。
- 修正快速切换角色时未及时保存、跨项目标签恢复竞争和删除当前条目后的标签选中问题。
- 仅打开资料或切换编辑模式不再触发伪内容更新；滚动状态不因父组件重新渲染而跳回。

### 维护

- 更新记录改为在本文件积累，不再要求通过 GitHub Release 正文记录每次小改动；标签和 Release 不与小目标逐一绑定。

# OpenFic 个人开发 Roadmap

> 状态：个人长期开发路线与架构决策记录，随本仓库提交；这些功能主要服务于个人使用，不以合并 upstream 为前提。
> 维护策略：持续从 `syrizelink/OpenFic` 吸收上游更新，在本地功能分支小步开发；是否向上游提交 PR 按具体改动另行决定。
> 运行基线：本地用 `docker build -t openfic:local` 跑调试实例。

## AI 协作与全局讨论（当前设计与实施方向）

### 产品目标

让 OpenFic 在每个小说项目内同时服务两种不同的协作场景：

- **项目级讨论**：围绕已经确认的世界观、人物和情节长期、多轮地讨论设定与因果；讨论可追溯、可换模型，但默认不是 canon。
- **受信息边界约束的创作**：作者可让 AI 起草、续写、改写或润色正文，再亲自修改；当前写作 Agent 只能知道这一卷、这一章或这一视角允许知道的信息，避免剧透。

这不是“AI 永远只能提建议”。AI 可以生成正文草稿；它不能把讨论结论、人物状态、摘要或世界设定静默地写成长期项目事实。

### 必须保持的边界

| 内容 | 默认状态 | 能否进入写作 Agent 上下文 | 如何写入长期项目资料 |
| --- | --- | --- | --- |
| 全局讨论线程 | 探索性、非 canon | 否 | AI 可创建待审变更；作者集中查看、编辑并确认后写入 |
| 世界书、人物、已确认提纲与写作护栏 | 已确认项目资料 | 由当前写作范围决定 | 正常编辑或已确认的 diff |
| AI 正文草稿 / 润色结果 | 待作者编辑 | 仅在作者采用后成为正文 | 在编辑器中以草稿、插入或选区替换方式确认 |
| 摘要、人物状态、伏笔和记忆更新 | 待确认的维护提案 | 否，直到作者确认 | 进入变更列表，逐项显示 diff 并确认 |

- 世界书的 `is_enabled` 继续只服务于创作上下文的信息差控制，不能兼任讨论记录的开关。
- 写作模式的“看不见”必须在上下文组装和工具权限两处生效；不能只依赖提示词或 UI 隐藏。
- 每个讨论线程和创作会话记录所用模型。换模型以新建或分叉会话为主，不要求在同一上下文中无痕切换。
- 不做无人值守的正式资料写入：后台任务和讨论 Agent 可以生成待审变更，但不得绕过作者确认应用到项目资料。

### 多书项目范围

- 讨论线程、会话、引用、提案、确认动作和压缩摘要都必须带项目归属；不能为当前小说写死名称、资料类型或上下文规则。
- 项目切换是讨论与创作的最外层边界：默认不读取、检索或引用其他小说的资料和讨论。
- 不设计跨项目读取、共享资料或合并讨论；每个项目始终是独立边界。
- 路由、数据模型和权限检查以 `project_id` 为边界。当前仙侠项目只是验证样例，不是产品内置设定。

### 参考项目：借机制，不迁移底座

| 项目 | 借鉴内容 | 不采用的部分 |
| --- | --- | --- |
| OpenFic | 既有项目、模型提供商、持久化 Agent 会话、上下文压缩，以及世界书/人物/笔记按需读取能力 | 不把现有写作 Agent 的资料启用开关用于讨论管理 |
| xnovelist | AI 输出作为可接受或拒绝的提案；模型不自行拥有项目状态的写入权；明确区分 AI 能力范围 | 纯前端 IndexedDB、非聊天定位，以及“模型必须完全无状态”的实现方式 |
| OpenWrite | 项目内 AI chat 的交互位置；聊天建议进入编辑器的操作流；BYOK 模型选择体验 | Cloudflare/D1/账号工作区架构，以及以 Story Map 自动展开为主的创作流 |
| novel-agent | `bible`、大纲、摘要/记忆、运行记录与正文草稿的职责分离 | 以 `brainstorm → outline → write → review` 自动化管线作为主入口 |

不纳入近期路线：多 Agent 自动生产整本小说、自动将讨论写入 canon、依赖本机 Codex/Claude CLI 的模型适配层、或另起一套写作应用/数据存储。真实 Git 仓库集成也不作为写入安全边界；OpenFic 先实现数据库内的 Git 式暂存、确认和回滚闭环，外部 Git 只作为以后可选的已确认快照出口。

### 阶段 0：项目级待审变更闭环

目标：允许 AI 主动整理和修改内容，同时确保作者不会因漏看某段聊天而让未审内容污染正式资料。

当前进度：已完成项目级待审变更的存储、基础 API、状态筛选、数量接口，以及项目侧持续可见的收件箱（列表、详情、before/after、来源信息、拒绝确认）。编辑、批量确认应用、冲突处理和 Agent 创建待审变更尚未实现。

- AI 对世界书、人物、提纲、笔记、摘要和伏笔等长期资料的修改，先创建绑定 `project_id` 的待审变更，不直接修改正式表。
- 待审变更保存目标资料、修改前版本或内容哈希、修改后内容、来源会话/消息、模型、创建时间和状态。
- 提供项目级“待审变更”列表及持续可见的未审数量；作者不必回到原会话寻找散落的写入结果。
- 作者可以编辑、丢弃或选择若干变更后统一确认，并为这组变更填写或自动生成说明。
- 确认时检查正式资料是否已变化；基线不一致则显示冲突，不静默覆盖。
- 确认后的写入复用现有 revision/snapshot 机制，原子应用并保留回滚点；拒绝或未审内容不进入 Agent 的正式项目上下文。
- 人工在现有编辑器中的直接编辑保持原行为；所有由 AI 发起的长期资料写入统一经过待审变更入口。

验收：在两个独立项目中分别创建待审变更，列表和数量互不串线；未确认内容不能被正式资料读取工具看到；确认后内容可读且可通过现有 revision 机制回滚。

### 阶段 1：项目级讨论的最小闭环

目标：在一个 OpenFic 项目中创建并持续使用多个讨论线程。

- 为 Agent session 增加明确的 `discussion` profile，与既有写作 Agent 区分。
- 线程保存标题、消息、创建/更新时间、所用模型和项目归属；可打开旧线程继续讨论。
- 新建线程时从已有模型配置中选择模型；模型选择写入会话记录。
- 讨论 profile 先只读项目资料，没有直接修改世界书、人物、笔记、提纲或正文正式数据的工具；创建待审变更在待审闭环补齐后接入。
- UI 提供线程列表、当前会话和模型标识；不在这一阶段加入自动总结或自动检索整个讨论库。

当前进度：已增加只读 `Discuss` 主 Agent 和项目级“开始讨论”入口。讨论复用现有 Task/Agent session 持久化，保存 `project_id`、`agent_key` 和模型记录；打开旧任务时前端恢复原 Agent、模型和推理强度。可用现有任务列表创建、重开多个讨论线程。当前 Discuss 没有写工具，也尚不能创建待审变更，因此“讨论产生待审提案”仍留待后续闭环。

验收：关闭并重开应用后，可按原模型和原消息继续某一讨论；讨论 Agent 没有正式项目资料写入能力，后续即使接入提案也只能写入待审变更。

### 阶段 2：全局讨论的上下文与引用

目标：让讨论 Agent 能够理解已确认的项目上下文，而不是每次手工粘贴资料。

- 讨论中可手动附加世界书、人物、笔记、提纲或正文片段，并在消息旁保留引用来源。
- 讨论 Agent 保留现有按需读取项目资料的能力，但只读范围限定为已确认资料；“讨论记录”本身不是默认检索语料。
- 为一次回复展示或记录实际读取的资料，便于追溯结论依据。
- 先验证手动引用和按需读取是否足够；只有实际出现检索负担，才讨论对讨论记录的摘要或语义检索。

当前进度（增强版阶段 2 已完成）：

- 复用现有 `@` 手动引用和章节、摘要、世界书、笔记、人物按需读取工具；Discuss 只注册只读工具，没有写入或子 Agent 调度能力。
- canonical mention 在运行时按当前 `project_id` 解析；跨项目对象、隐藏笔记和禁用世界书条目只保留消息中原有的 fallback 标签，候选搜索也不再显示禁用世界书条目。
- 世界书读取工具只读取当前项目已启用条目；其他读取工具继续使用既有项目范围和隐藏状态检查。
- 实际工具调用及其资料标题沿用现有会话消息持久化和工具卡片显示；Discuss 回复提示词同时要求列出实际来源并承认未读取的信息边界。
- 复用现有全局规则 + 当前项目规则注入机制，并增加跨项目隔离回归；没有另建规则子系统。
- Discuss 使用小说讨论专用压缩格式，分别保留已确认事实、线程暂定结论、候选方案、未决问题、线程放弃方案、来源和用户约束；压缩摘要仍是会话状态，不会升格为 canon。
- 尚未加入讨论库自动检索、讨论间共享记忆、待审变更确认应用或真实模型端到端调用。

验收：同一设定问题可追溯到具体资料来源；尚未确认的讨论不会被后续写作 Agent 自动带入。

### 阶段 3：受信息边界约束的创作协作

目标：降低作者的提示词负担，同时保留创作中的信息差。

- 为创作会话明确任务入口：起草当前场景、续写、按节拍改写、润色选区、连续性检查。
- 由系统根据当前章节、卷提纲、允许资料和作者选择的任务组装上下文；作者只补充目标、语气或长度等必要信息。
- 将当前资料启用范围升级为明确的写作 scope（例如卷/章/视角/编辑检查），并在上下文构造和读取工具中统一执行。
- AI 生成正文时，输出为可编辑草稿、插入建议或选区替换预览；作者决定是否采用。
- 创作会话不得隐式更新世界书、人物状态、摘要、伏笔或提纲。

验收：对同一正文可在“作者/编辑检查”与“受限视角创作”范围间切换；受限范围无法经提示词或读取工具取得隐藏设定。

### 阶段 4：讨论与创作结果的受控升格

目标：把真正定下来的内容方便地转入正式资料，而不把探索误写成事实。

- 从讨论消息或创作结果中选择片段，指定目标：世界书、人物、提纲、笔记、摘要或伏笔记录。
- 系统生成针对目标资料的 diff，显示来源线程/消息、模型和预期影响；作者确认后才执行写入。
- 对摘要、人物状态和伏笔等易漂移资料，统一进入待确认变更列表；不允许自动提交。
- 被拒绝的提案保留在会话轨迹中，但不被标记为“项目已否定的 canon”。

验收：每一项项目资料的 AI 来源都能回链到会话和确认动作；拒绝或未确认的讨论不会改变后续 Agent 的项目事实。

### 阶段 5：追溯、压缩与长期使用校验

目标：在长期讨论和实际写作后，验证系统没有用便利性换来上下文污染。

- 会话可查看模型、引用、提案、确认动作和对应 diff；必要时可从旧消息重新进入讨论。
- 对过长会话采用可见、可回溯的压缩摘要；摘要仍属于会话状态，不自动成为 canon。
- 用真实场景检查：全局设定讨论、受限视角续写、AI 初稿后人工修改、人工段落润色、拒绝维护提案。
- 只有在上述流程稳定后，才评估是否需要讨论库检索、模型路由或新的外部 Agent 适配器。
